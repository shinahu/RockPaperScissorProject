export interface PlayerChoice {
    playerChoice: string;
    userName: string;
}

export interface Results {
    playerChoice: string;
    cpuChoice: string;
    gameResult: string;
    userName: string;
}

export interface RoundChoice {
    roundChoice: string;
    userName: string;

}